#!/usr/bin/env zsh

if ! command -v 'which bat' &> /dev/null
then
    alias cat='bat -p'
    export MANPAGER='bat'
fi

if ! command -v 'nvim --version' &> /dev/null
then
    alias vim='nvim'
    alias vi='nvim'
fi

if [ -d "$HOME/.cargo" ]
then
	source "$HOME/.cargo/env"
fi