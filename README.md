# .dotfiles

run the following command to begin,

```bash
sh -c "$(curl --proto '=https' --tlsv1.2 -LsSf https://dub.sh/psadi/bootstrap)"
```

> **Warning:** Always review the contents of any script before executing it. Do not run random scripts from the internet without understanding what they do.
